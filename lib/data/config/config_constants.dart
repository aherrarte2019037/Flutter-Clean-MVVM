class ConfigConstants {
  ConfigConstants._();

  static const authBaseUrl = 'https://flutter-clean.free.beeceptor.com/';
  static const connectionCheckerInterval = Duration(seconds: 15);
  static const connectionCheckerTimeout = Duration(seconds: 10);
}
